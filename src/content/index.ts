// Tab Nest content script
// Injected into all pages at document_idle.
// Minimal footprint — this file intentionally stays near-empty in Phase 2.
// Future phases will add message listeners for tab metadata extraction.

console.log('Tab Nest content script loaded');
